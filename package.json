{
  "name": "twitch-telebot",
  "module": "src/index.ts",
  "type": "module",
  "devDependencies": {
    "bun-types": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "compile": "bun build --outfile ./twitch-buzz-bot --compile ./src/index.ts",
    "start": "./twitch-buzz-bot"
  },
  "dependencies": {
    "@kararty/dank-twitch-irc": "^6.0.0",
    "telegraf": "^4.15.0"
  }
}
